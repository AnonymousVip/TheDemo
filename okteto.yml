name: thedemo
build:
  thedemo:
    context: .
    dockerfile: Dockerfile

dev:
  thedemo:
    image: okteto/python:3
    command: bash
    sync:
      - .:/usr/src/app
    forward:
      - 8080:8080
    reverse:
      - 9000:9000
    autocreate: true
    volumes:
      - /root/.cache/pip
